<div class="w-full flex border-b <%= @with_margin ? "mb-4" : "" %> <%= @small ? "pt-8 pb-5" : "py-12" %> items-center">
  <div class="w-8/12">
    <% if @link %>
      <%= link_to @link, class: "hover:underline" do %>
        <h2 class="<%= @small ? "text-3xl" : "text-4xl" %> font-bold"><%= @title %></h2>
      <% end %>
    <% else %>
      <h2 class="<%= @small ? "text-3xl" : "text-4xl" %> font-bold"><%= @title %></h2>
    <% end %>
    <% if @subtitle %>
      <h3 class="text-sm text-gray-600 font-normal mt-1">
        <%= @subtitle %>
      </h3>
    <% end %>
  </div>
  <div class="w-4/12 flex justify-end">
    <%= content %>
  </div>
</div>