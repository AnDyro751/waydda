var element = document.querySelector("#order_<%= @order.id.to_s %>");
if (element) {
    element.parentNode.removeChild(element);
}
window.addToastify("primary", "Se ha enviado una notificación al comprador");