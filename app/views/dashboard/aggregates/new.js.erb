if (window.vanillaModal) {
    var element = document.querySelector("#modal");
    if (element) {
        console.log("ABRIR MODAL");
        window.current_modal.open("#modal");
        document.querySelector(".modal-content").innerHTML = "<%= j render Dashboard::Aggregates::Form.new(aggregate: @aggregate, product: @product,aggregate_category: @aggregate_category) %>";
    }
}
