<%= simple_form_for(@aggregate, url: action == "update" ? dashboard_product_aggregate_path(@product, @aggregate) : dashboard_product_aggregates_path(@product)) do |f| %>
  <%= f.input :name %>
  <%= f.input :price, as: :float %>
  <%= f.input :description %>
  <%= f.input :default, as: :boolean %>
  <%= f.submit %>
<% end %>