if (window.vanillaModal) {
    var element = document.querySelector("#modal");
    if (element) {
        console.log("ABRIR MODAL");
        console.log("<%= request.original_url %>")
        window.current_modal.open("#modal");
        document.querySelector(".modal-content").innerHTML = "<%= j render Dashboard::Aggregates::Form.new(aggregate: @aggregate) %>";
        window.customLazyLoad.update();
        document.querySelector("body").classList.toggle("overflow-y-hidden");
    }
}
//var modal = new VanillaModal();
//modal.open();
