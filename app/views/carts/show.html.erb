<header class="px-6 py-4 bg-white border-b">
  <img
    draggable="false"
    src="<%= get_image_url(20, 100, "utils/w-logo-5.png") %>"
    class="h-10"
    />
</header>
<%= react_component('ShowCart', props: {total: @total, current_cart: @current_cart, items: @items, intent_id: @intent}, prerender: false) %>

<%= action_name %><%= controller_name %>