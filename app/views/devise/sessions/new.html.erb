<div class="w-full justify-center min-h-screen flex items-center">
  <div class="w-4/12 flex flex-wrap">
    <div class="w-full flex items-center">
      <img
        class="h-10 lazy w-auto"
        src="<%= get_image_url(20, 100, "utils/w-logo-5.png") %>"
        alt="Waydda logo"
      >
      <h3 class="ml-2 font-normal">Waydda</h3>
    </div>
    <%= simple_form_for(resource,
                        defaults: {
                            input_html: {class: "#{input_color_class} mt-3 mb-4", type: "tel"},
                            label_html: {class: label_color_class}
                        },
                        as: resource_name, remote: true, html: {class: "w-full"}, url: session_path(resource_name)) do |f| %>
      <h1 class="w-full my-4 text-2xl font-bold">Inicia sesión en tu cuenta de Waydda</h1>
      <%#= render "devise/shared/error_messages", resource: resource %>
      <%= f.input :phone,
                  label: "Número de teléfono",
                  placeholder: "+52 552 471 4512",
                  required: "required"
      %>

      <div class="hidden" id="verification-code">
        <%= f.input :verification_code,
                    placeholder: "487878",
                    input_html: {value: ""},
                    label: "Código de verificación"
        %>
      </div>
      <%= f.input :remember_me, as: :boolean, label: "Recordar sesión" %>
      <%#= f.label :remember_me %>
      <%= f.submit "Iniciar sesión", data: {disable_with: "Cargando..."}, class: "#{main_color_button(color: "primary", padding: "big")} w-full" %>
    <% end %>
  </div>
</div>
